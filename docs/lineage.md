# Lineage (docs-only)
Column‑level lineage conceptually tracked from bronze→gold using transformation metadata. Evidence links are included in the daily manifest (see `samples/deliverables/manifest_YYYY-MM-DD.json`).
